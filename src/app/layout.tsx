import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Navbar from "@/components/layout/Navbar";
import Footer from "@/components/layout/Footer";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "FinSync — AI-Powered Finance Dashboard for Teams",
  description:
    "Track, analyze, and optimize your team finances with AI-powered insights. Real-time dashboards, smart reports, and seamless integrations.",
  openGraph: {
    title: "FinSync — AI-Powered Finance Dashboard for Teams",
    description:
      "Track, analyze, and optimize your team finances with AI-powered insights.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.variable} antialiased`}>
      <body className="min-h-screen flex flex-col">
        <Navbar />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
